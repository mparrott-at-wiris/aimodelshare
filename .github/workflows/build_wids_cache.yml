name: Build WiDS Prediction Cache Artifact (Resumable)

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read      # Required to list/download artifacts
  checks: write

jobs:
  build-wids-cache-chunk:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install Dependencies
        run: pip install pandas numpy scikit-learn joblib

      # Restore Checkpoint
      # UPDATED: Changed name to 'v2' to avoid crashing on the old 2GB artifact
      - name: Restore Checkpoint
        uses: dawidd6/action-download-artifact@v6
        with:
          name: wids-cache-checkpoint-v2
          path: .
          search_artifacts: true
          workflow_conclusion: "" 
          if_no_artifact_found: warn
        continue-on-error: true

      # Debug Step
      - name: Debug Checkpoint
        run: |
          if [ -f "wids_cache_checkpoint.jsonl.gz" ]; then
            echo "✅ SUCCESS: Compressed checkpoint found!"
            ls -lh wids_cache_checkpoint.jsonl.gz
          else
            echo "⚠️ NOTE: No checkpoint found. This is normal for the first v2 run."
          fi

      # Run the script
      # Note: We keep thread limits = 1 because the Python script now handles
      # parallelism via n_jobs=2. This prevents CPU over-subscription.
      - name: Run Compute Chunk
        run: |
          export OMP_NUM_THREADS=1
          export OPENBLAS_NUM_THREADS=1
          python precompute_wids_cache.py

      # Save Checkpoint (Runs even if timeout occurs)
      # UPDATED: Saves to 'v2' artifact name
      - name: Save Checkpoint
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: wids-cache-checkpoint-v2
          path: wids_cache_checkpoint.jsonl.gz
          retention-days: 5

      # Save Final Result (Only if the script finishes 100%)
      - name: Save Final Artifact
        if: hashFiles('wids_prediction_cache.json.gz') != ''
        uses: actions/upload-artifact@v4
        with:
          name: wids-prediction-cache-file
          path: wids_prediction_cache.json.gz
          retention-days: 90
